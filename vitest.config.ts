// vitest.config.ts (for TypeScript)
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    // You can add global test configuration here
    // For example, you can specify the environment:
    // environment: 'node',
  },
});
